# Minimal requirements for Render deployment (512MB RAM limit)
# Excludes: sentence-transformers (200MB), torch (150MB), cross-encoder
# Uses: HuggingFace Inference API for embeddings (0MB)

# Core Framework
fastapi==0.122.0
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.12.0
python-multipart==0.0.20

# LangChain & RAG (without heavy local model dependencies)
langchain==1.1.0
langchain-core==1.1.0
langchain-community==0.4.1
langchain-ollama==1.0.0
langchain-huggingface==1.1.0
langchain-groq==1.1.0

# Vector Database (without local embedding models)
chromadb==1.3.5
langchain-chroma==1.0.0
rank_bm25==0.2.2

# Document Processing
pypdf==6.4.0
python-docx==1.2.0

# Utilities
python-dotenv==1.2.1
redis==7.1.0
