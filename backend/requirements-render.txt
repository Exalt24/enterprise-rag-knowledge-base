# Lightweight requirements for Render (512MB RAM, CPU-only)
# Install CPU-only PyTorch to avoid 700MB+ CUDA bloat

--extra-index-url https://download.pytorch.org/whl/cpu

# Core Framework
fastapi==0.139.0
uvicorn[standard]==0.38.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
python-dotenv==1.2.2

# LangChain
langchain==1.3.12
langchain-core==1.4.9
langchain-community==0.4.2
langchain-groq==1.1.3
langchain-ollama==1.1.0
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2

# Qdrant (remote vector storage)
qdrant-client==1.18.0
langchain-qdrant==1.1.0

# Embeddings (local Sentence Transformers with CPU-only PyTorch)
sentence-transformers==5.6.0
torch  # CPU-only via extra-index-url above
transformers==5.13.0

# Search & Retrieval
rank-bm25==0.2.2

# Document Processing
pypdf==6.14.2
python-docx==1.2.0

# Redis
redis==8.0.1

# Utilities
requests==2.34.2
